using HashFunctions;

namespace HashFunctions {
    public class HashTable {
        public HashTable(int tableSize, ) {
            //Create array for all lists
            int[] array1 = new int[tableSize];
        }
        
        public int get(Int64 x) {
            return array1[x];
        }
        public void set(Int64 x, int v){
            
            List<int> listEmpty = new List<int>();
            array1[] = 
            return 
        }

        public void increment(Int64 x, int d){

        }

    }
}